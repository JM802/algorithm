## 仓库性质

这是一个算法学习仓库，包含 LeetCode 题解、课程实验代码、算法作业。用户是正在学习算法的学生，使用语音输入为主（输入可能有口语化表达、断句不清、错别字），需要你帮他梳理思路、纠正理解。

## 核心交互模式

用户经常会用语音描述自己对某个算法/题目的理解，你需要：

1. **判断掌握程度**：根据用户的描述，指出他理解中正确和错误的部分，不要笼统地说"理解得不错"，要具体到哪个点对、哪个点错。
2. **给出正确思路**：如果用户理解有误，用清晰的逻辑给出正确的思考路径，而不是直接甩答案。
3. **更新笔记**：用户会要求把新的理解或纠正后的内容合并到现有的 Markdown 笔记中。更新时保持原有笔记风格和结构，不要重写整个文件，只做增量修改。
4. **检查错误**：主动检查用户笔记中的技术错误并指出。

## 笔记组织原则

- **按题型/算法分类，不按单个题目罗列**。每种题型给出通用思路和递推公式，典型题目只列题号。
- **笔记风格统一**：参考 `backtracing.md` 的结构 — 先讲核心框架，再讲本质/原理，然后按题型分类，最后是易错点和题目索引。
- **强调思路和算法**，不写具体题目的完整代码（代码在 `.cpp` 文件里）。

## 重要笔记文件

| 文件                                                    | 内容                                                                      |
| ------------------------------------------------------- | ------------------------------------------------------------------------- |
| `leetcode/backtracing/backtracing.md`                 | 回溯四大步骤、单集合/多集合遍历、去重三种方法、切割问题、排列问题、解数独 |
| `leetcode/dynamic_programming/dynamic_programming.md` | DP 四步骤、斐波那契型、二维网格路径、0/1背包、完全背包、子集问题          |
| `leetcode/graph/dfs_and_bfs.md`                       | DFS/BFS 两种套路对比、回溯本质、层序遍历、拓扑排序                        |
| `leetcode/tree/回溯.md`                               | 回溯模版、三种常见套路（进门直接处理、标准模版、值传递）                  |
| `leetcode/tree/遍历相关逻辑.md`                       | 前中后序遍历（引用/值传递）、层序遍历                                     |
| `leetcode/greedy/greedy.md`                           | 贪心算法本质、与动态规划的区别                                            |

## 代码风格

- C++ 为主，使用标准库（vector, queue, stack 等）
- 文件命名：`leetcode{题号}_{简要描述}.cpp`，如 `leetcode77_combination.cpp`
- 代码结构：全局变量存结果，Solution 类包裹，backtracking/dfs/bfs 作为成员函数

## 注意事项

- 用户的语音输入经常有错别字或口语化表达（如"回字算法"→回溯算法、"减脂"→剪枝、"税是"→所以是），需要你根据上下文推断真实含义，不要字面理解。
- 用户倾向于用中文交流，笔记也是中文，保持中文回复。
